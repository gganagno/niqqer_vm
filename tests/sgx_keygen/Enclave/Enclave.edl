enclave {

    trusted {
        /* define ECALLs here. */
        public void print_key(int id);
        public int keygen();
        public void get_key(int id, [out, in]char got[16]);
    };

    untrusted {
        /* define OCALLs here. */

        void print_data([in,out]char got[16]);
        void ocall_print([out, in, string]char* value);
    };
};
